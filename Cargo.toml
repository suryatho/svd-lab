[workspace]
resolver = "2"
members = ["core", "loaders"]

[workspace.package]
version = "0.1.0"
edition = "2024"

[workspace.dependencies]
svd-core = { path = "core" }
svd-loaders = { path = "loaders" }
